name: bus_booking_app
description: A modern Flutter-based bus seat booking app.

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  firebase_core: ^2.28.0
  firebase_auth: ^4.18.0
  cloud_firestore: ^4.15.0
  firebase_storage: ^11.6.0
  firebase_database: ^10.4.0
  file_picker: ^6.2.0
  csv: ^5.0.2
  google_maps_flutter: ^2.6.0
  printing: ^5.12.0
  pdf: ^3.10.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
